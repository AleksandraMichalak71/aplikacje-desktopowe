<Window x:Class="aplikacje_desktopowe_zad3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:aplikacje_desktopowe_zad3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Podaj imię i nazwisko:" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                 Text="{Binding ImieNazwisko, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Podaj datę urodzin (dd.MM.yyyy):" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                 Text="{Binding DataUrodzin, Converter={StaticResource DateTimeToStringConverter}, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Twoje pierwsze imię:" Margin="0,0,8,4"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PierwszeImie}" Margin="0,0,0,4"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Twoje nazwisko:" Margin="0,0,8,4"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Nazwisko}" Margin="0,0,0,4"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Twój wiek:" Margin="0,0,8,4"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Wiek}" Margin="0,0,0,4"/>
    </Grid>
</Window>
